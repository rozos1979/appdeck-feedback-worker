{
  "name": "appdeck-feedback-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker: receives AppDeck in-app feedback, creates GitHub issues in rozos1979/appdeck-feedback",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.78.0"
  }
}
